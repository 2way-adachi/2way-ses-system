# 画面設計

MVPで想定する画面構成を整理する。

- 作成日: 2026-08-03
- 更新日: 2026-08-17（SESメニュー再整理、メンバー詳細タブ構成を反映）
- ステータス: ドラフト。確定仕様ではない。

---

## 1. 共通レイアウト

**Lavender（2way Manage system）の既存レイアウトをそのまま利用する。新規に作成しない。**

- 上部ヘッダー（AppBar「2way Manage system」）
- 左側サイドメニュー（Drawer 幅240px、PCは常時表示・SPはハンバーガー）
- メインコンテンツ（Container）

### サイドメニュー

FUKURO2.0とは別に、大項目「**SES**」を新設して以下を配置する。

```text
SES
  案件メール一覧
  要員メール一覧
  メンバー一覧
  マッチング一覧
  提案管理一覧
```

配置位置・実装方法は [Lavender組み込み要件](../requirements/lavender-integration.md) を参照。

### スキルシートの位置づけ

スキルシートは独立したSESメニューとしては表示せず、メンバー詳細画面内の「スキルシート」タブで表示、編集する。

メンバー一覧からメンバー詳細へ遷移し、メンバー詳細内で info、スキルシート、提案、マッチングを横断して確認できる構成とする。

なお、SESメニュー自体は権限で出し分けず**常時表示**する。

### 前提

- PCブラウザを主対象とする
- **Material-UI v4** のコンポーネントを利用する（Lavender既存に準拠）
- パンくずはLavenderに既存の仕組みがないため、必要性を含めて工程1で判断する

---

## 2. 画面一覧

| # | 画面 | 区分 |
|---|------|------|
| 1 | 案件メール一覧 | 必須 |
| 2 | 案件詳細、編集 | 必須 |
| 3 | 案件メール取り込み | 必須 |
| 4 | 要員メール一覧 | 必須 |
| 5 | 要員詳細、編集 | 必須 |
| 6 | 要員メール取り込み | 必須 |
| 7 | メンバー一覧 | 必須 |
| 8 | メンバー詳細 | 必須 |
| 9 | メンバー詳細 info タブ | 必須 |
| 10 | メンバー詳細 スキルシートタブ | 必須 |
| 11 | メンバー詳細 提案タブ | 必須 |
| 12 | メンバー詳細 マッチングタブ | 必須 |
| 13 | マッチング一覧 | 必須 |
| 14 | 提案管理一覧 | 必須 |

---

## 3. 案件メール一覧画面

案件メールから取得した案件を一覧表示する。

### 表示項目候補

- 案件名
- 必須スキル
- 勤務地
- 単価
- 開始時期
- 送信元
- 受信日時
- 登録日時

### 操作

- 詳細表示
- 編集
- メール取り込み

---

## 4. 要員メール一覧画面

要員メールから取得した要員を一覧表示する。

### 表示項目候補

- 要員名
- 主なスキル
- 最寄駅
- 単価
- 稼働開始時期
- 所属
- 送信元
- 受信日時
- 登録日時

### 操作

- 詳細表示
- 編集
- メール取り込み

---

## 5. メンバー一覧画面

メンバーの一覧を表示する。メンバー一覧の行を押下するとメンバー詳細へ遷移する。

### 表示項目候補

- メンバー名
- 年齢
- 最寄駅
- 主なスキル
- 就業状況
- 勤怠提出元
- 提案中件数
- マッチング件数
- 最終更新日時

就業状況は、職歴の「現在参画中」と終了年月から算出した値を表示する。選択肢は「稼働中」「待機中」「稼働中（終了予定あり）」とする。

勤怠提出元は、現在参画中の職歴、または最新の職歴の値を表示、検索対象にする。過去の全職歴を横断して勤怠提出元検索にヒットさせない。

### 操作

- 新規作成
- メンバー詳細表示
- Excelインポート

---

## 6. メンバー詳細画面

メンバーの情報を横断して確認する画面。以下のタブを持つ。

1. info
2. スキルシート
3. 提案
4. マッチング

### 6.1 info タブ

メンバーの基本情報と現在の営業状況サマリーを表示する。

### 表示項目候補

- 氏名
- 年齢
- 性別
- 最寄駅
- 所属
- 希望単価
- 稼働開始時期
- 就業状況
- 提案中件数
- マッチング件数

### 6.2 スキルシートタブ

メンバーのスキルシート内容を表示、編集する。従来のスキルシート詳細、編集機能はこのタブ内に集約する。

### 表示項目候補

- 基本情報
- スキルサマリー
- 資格
- 職歴一覧
- 自己PR

### 操作

- 編集
- PDF表示
- 複製して新規作成

### 6.3 提案タブ

メンバーに紐づく提案状況を表示する。提案管理一覧で提案を押下した場合、このタブを表示する。

### 表示項目候補

- 案件名
- 必須スキル
- 提案先
- 次回アクション日
- 進捗ステータス
- メモ

### 6.4 マッチングタブ

メンバーに紐づくマッチング結果を表示する。マッチング一覧でマッチングを押下した場合、このタブを表示する。

### 表示項目候補

- 案件名
- 必須スキル
- 案件開始時期
- 点数
- ステータス

### 操作

- マッチング承認
- 見送り
- 案件詳細表示

### 表示方針

Excelの帳票をそのまま再現するのではなく、ブラウザで読みやすいカード、タグ、表形式を利用する。

職歴ごとに勤怠提出元（上位会社 / 参画中メンバー）を表示する。

最終更新日時は `yyyy-mm-dd hh:mm` 形式で表示し、秒は表示しない。

---

## 7. スキルシート編集機能

メンバー詳細の「スキルシート」タブ内、またはそこから遷移する編集状態として提供する。新規作成と既存スキルシート編集を兼用する。

以下のタブまたはセクションに分ける。

1. 基本情報
2. スキル、資格
3. 職歴
4. 自己PR

職歴は複数件追加可能にする。

基本情報、就業・希望条件では以下の方針とする。

- 社員番号は半角英数字のみ入力できるようにする
- 氏名、フリガナには入力例を表示する
- 就業状況は入力項目として持たず、職歴から自動判定する
- 転職意向は入力項目から削除する
- 勤怠提出元は就業・希望条件ではなく、職歴ごとに入力する

スキル、資格では以下の方針とする。

- 年月入力は `yyyymm` の半角数字入力から `yyyy-mm` へ自動整形する
- スキルの最終利用にも入力例を表示する
- 資格の入力順は「資格名」「取得年月」「備考」とする

職歴では以下の方針とする。

- 開始年月、終了年月は `yyyymm` の半角数字入力から `yyyy-mm` へ自動整形する
- 現在参画中チェックの有無にかかわらず、終了年月は入力可能にする
- 現在参画中チェックを操作しても、入力済みの終了年月は削除しない
- 現在参画中チェックあり、かつ終了年月ありの場合は「稼働中（終了予定あり）」として扱う
- 勤怠提出元を職歴ごとに入力できるようにする

入力補助、入力チェックでは以下の方針とする。

- 日付、年月など桁数が確定する入力欄は、入力完了後に次の欄へ自動移動する
- 日付、年月は全角数字を半角へ変換し、区切り文字を自動入力する
- 13月、32日など成立しない日付、年月はエラー表示し、その欄に留める
- 職歴の開始年月と終了年月は、開始年月 <= 終了年月であることを検証する

保存処理では以下の方針とする。

- 新規作成、編集ともに保存できるようにする
- モック段階ではブラウザローカル保存で挙動確認し、本実装ではDB/API保存へ置き換える
- 保存時に最終更新日時を `yyyy-mm-dd hh:mm` 形式で更新する

### 下書き保存

スキルシート編集画面では、入力途中の内容を下書きとして扱う。

- 編集中は1分おきに下書きへ自動保存する
- **新規作成中は自動保存しない。** 一度「保存する」で正式保存した以降の編集から有効になる
- 「保存する」押下時は、現在の入力データを正式保存し、下書きを破棄する
- 「キャンセル」押下時は、入力途中の内容を正式保存せず、下書きを破棄する
- 下書きが残っている状態で編集を中断した場合、次回編集画面を開いたときに下書き復元確認ポップアップを表示する
- 復元確認で「はい」を押下した場合、下書き内容を入力欄へ反映する
- 復元確認で「いいえ」を押下した場合、下書きを破棄し、正式保存済みデータを表示する

### 保存中ステータス表示

自動保存の状態は、**画面右下に固定表示**する（2026-08-04 決定）。

- 小さく表示し、編集操作を妨げない
- Material-UI v4 の Snackbar 等、既存コンポーネントで実現する
- 表示内容は「保存中」「保存しました（時刻）」程度にとどめる
- **自動保存の失敗はここに表示しない。** エラーを出さず次回タイミングで再試行する
- 新規作成中は自動保存を行わないため、この表示も出さない

### 下書き復元確認ポップアップ

| 操作 | 挙動 |
|------|------|
| はい | 下書きの内容を入力欄に反映する |
| いいえ | 下書きを破棄し、正式保存済みデータを入力欄に表示する |

---

## 8. 案件詳細、編集画面

### 表示項目候補

- 案件名
- 必須スキル
- 勤務地
- 単価
- 開始時期
- 送信元
- 受信日時
- 登録日時

### 表示、編集候補

- 案件名
- メール件名
- メール本文
- 必須スキル
- 勤務地
- 単価
- 開始時期
- 送信元
- 受信日時

抽出誤りを画面から修正できることをMVPの完成条件とする。

---

## 9. メール取り込み画面

- メール取り込み実行ボタン
- 最終実行日時
- 成功件数
- 失敗件数
- 取り込み履歴
- エラー内容

MVPではリアルタイム監視を行わず、この画面のボタンによる手動実行を基本とする。

---

## 10. マッチング一覧画面

要員と案件をマッチングし、結果を一覧で表示する。

### 表示項目候補

- 案件名
- 必須技術
- 案件開始時期
- 要員名
- 点数

### 操作

- マッチング行を押下すると、該当メンバー詳細の「マッチング」タブを表示する

---

## 11. 提案管理一覧画面

マッチングで承認した提案を一覧で表示する。

### 表示項目候補

- 案件名
- 必須技術
- 案件開始時期
- 要員名
- 次回アクション日
- 進捗ステータス

### 操作

- 提案行を押下すると、該当メンバー詳細の「提案」タブを表示する
- 提案詳細はメンバー詳細の提案タブ内で確認、更新する

---

## 12. 未確定事項

- 一覧画面の検索、絞り込み条件
- ページングの方式と件数（Lavender既存画面の方式に揃えるかを含む）
- パンくずの要否

UIフレームワーク（Material-UI v4）とログイン方式はLavender既存に準拠するため確定済み。

---

## 関連ドキュメント

- [MVP要件定義](../requirements/mvp-requirements.md)
- [Lavender組み込み要件](../requirements/lavender-integration.md)
- [スキルシート要件](../requirements/skill-sheet-requirements.md)
- [データモデル](data-model.md)

