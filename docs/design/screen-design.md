# 画面設計

MVPで想定する画面構成を整理する。

- 作成日: 2026-08-03
- 更新日: 2026-08-04（Lavender組み込み方針・スキルシート初期表示条件を反映）
- ステータス: ドラフト。確定仕様ではない。

---

## 1. 共通レイアウト

**Lavender（2way Manage system）の既存レイアウトをそのまま利用する。新規に作成しない。**

- 上部ヘッダー（AppBar「2way Manage system」）
- 左側サイドメニュー（Drawer 幅240px、PCは常時表示・SPはハンバーガー）
- メインコンテンツ（Container）

### サイドメニュー

FUKURO2.0とは別に、大項目「**SES**」を新設して以下を配置する。

```text
SES
  スキルシート
  案件一覧
```

配置位置・実装方法は [Lavender組み込み要件](../requirements/lavender-integration.md) を参照。

### スキルシートメニュー押下時の初期表示

左側サイドメニューの「スキルシート」を開いたときは、ログインユーザーの権限とスキルシート作成状況に応じて初期表示を出し分ける。

| 条件 | 初期表示 |
|------|----------|
| 管理者権限を持つユーザー | スキルシート一覧画面 |
| 管理者権限を持たないユーザー、かつ自分のスキルシート作成済み | 自分のスキルシート詳細画面 |
| 管理者権限を持たないユーザー、かつ自分のスキルシート未作成 | 新規スキルシート作成画面 |

非管理者ユーザーに対しては、自分以外のスキルシート一覧を初期表示しない。

### 前提

- PCブラウザを主対象とする
- **Material-UI v4** のコンポーネントを利用する（Lavender既存に準拠）
- パンくずはLavenderに既存の仕組みがないため、必要性を含めて工程1で判断する

---

## 2. 画面一覧

| # | 画面 | 区分 |
|---|------|------|
| 1 | スキルシート一覧 | 必須 |
| 2 | スキルシート詳細 | 必須 |
| 3 | スキルシート編集（新規作成兼用） | 必須 |
| 4 | Excelインポート | 必須 |
| 5 | 案件一覧 | 必須 |
| 6 | 案件詳細、編集 | 必須 |
| 7 | メール取り込み | 必須 |
| 8 | 仮マッチング | 余力がある場合のみ |

---

## 3. スキルシート一覧画面

管理者権限を持つユーザーが「スキルシート」を開いた場合の初期表示画面とする。

### 表示項目候補

- 氏名
- 年齢
- 最寄駅
- 主なスキル
- 経験年数
- 就業状況
- 勤怠提出元
- 最終更新日

### 操作

- 新規作成
- 詳細表示
- 編集
- Excelインポート

---

## 4. スキルシート詳細画面

管理者権限を持たないユーザーが「スキルシート」を開き、すでに自分のスキルシートを作成済みの場合は、この画面に自分のスキルシート内容を表示する。

### 表示セクション

- 基本情報
- スキルサマリー
- 資格
- 職歴一覧
- 自己PR

### 操作

- 編集
- PDF表示
- 複製して新規作成

### 表示方針

Excelの帳票をそのまま再現するのではなく、ブラウザで読みやすいカード、タグ、表形式を利用する。

参画中メンバーについては、基本情報内に勤怠提出元（上位会社 / 参画中メンバー）を表示する。

---

## 5. スキルシート編集画面

新規作成と既存スキルシート編集を兼用する。管理者権限を持たないユーザーが「スキルシート」を開き、まだ自分のスキルシートを作成していない場合は、新規作成状態でこの画面を表示する。

以下のタブまたはセクションに分ける。

1. 基本情報
2. スキル、資格
3. 職歴
4. 自己PR

職歴は複数件追加可能にする。

基本情報では、参画中メンバー向けの項目として勤怠提出元を選択できるようにする。

---

## 6. 案件一覧画面

### 表示項目候補

- 案件名
- 必須スキル
- 勤務地
- 単価
- 開始時期
- 送信元
- 受信日時
- 登録日時

---

## 7. 案件詳細、編集画面

### 表示、編集候補

- 案件名
- メール件名
- メール本文
- 必須スキル
- 勤務地
- 単価
- 開始時期
- 送信元
- 受信日時

抽出誤りを画面から修正できることをMVPの完成条件とする。

---

## 8. メール取り込み画面

- メール取り込み実行ボタン
- 最終実行日時
- 成功件数
- 失敗件数
- 取り込み履歴
- エラー内容

MVPではリアルタイム監視を行わず、この画面のボタンによる手動実行を基本とする。

---

## 9. 仮マッチング画面

**余力がある場合のみ作成する。**

- 案件情報
- 候補者一覧
- 一致スキル
- 一致件数
- 不一致スキル

案件の必須スキル文字列と、スキルシートに登録された技術情報を単純比較し、一致件数順に並べる。

---

## 10. 未確定事項

- 一覧画面の検索、絞り込み条件
- ページングの方式と件数（Lavender既存画面の方式に揃えるかを含む）
- パンくずの要否

UIフレームワーク（Material-UI v4）とログイン方式はLavender既存に準拠するため確定済み。

---

## 関連ドキュメント

- [MVP要件定義](../requirements/mvp-requirements.md)
- [Lavender組み込み要件](../requirements/lavender-integration.md)
- [スキルシート要件](../requirements/skill-sheet-requirements.md)
- [データモデル](data-model.md)
