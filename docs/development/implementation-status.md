# 実装現状の記録（SES機能）

Lavender／thyme上の実装の現状を記録する文書。**要件ではなく事実の記録**。

- 作成日: 2026-08-17
- 前提: 実装が要件更新（2026-08-17のSESメニュー再整理）より先に進んでいたため、
  実装と最新要件には乖離がある。各要件文書には概要の注記を置き、詳細は本書に集約する
- 実装はすべて**未コミット**（Lavender `Feature/skill-sheet`、thyme `feature/skill-sheet`）

---

## 1. 最新要件と実装の対応表

2026-08-17要件（メニュー再整理）と実装の対応。**要件側の用語「メンバー」は実装では「要員（personnel）」**。

| 最新要件 | 実装の現状（2026-08-17更新） | 乖離 |
|---------|-----------|------|
| 大項目「SES」1つ・5メニュー | **追従済み**（大項目「SES」1つ化・5メニュー。暫定スキルシート項目も撤去済み） | 解消 |
| 案件メール一覧（`/project-mail`） | `/project-mail` にパス改名済み。画面モックのみ（API・バッチなし） | API・バッチ未実装 |
| 要員メール一覧（`/staff-mail`） | `/staff-mail` にパス改名済み。画面モックのみ | 同上 |
| メンバー一覧・詳細（`/member/:id?tab=`） | `/member`・`/member/:id?tab=info|skill-sheet|matching|proposal` に改名・クエリ対応済み | タブ名の表記（適合案件等）とスキルシートタブの中身のみ残 |
| メンバー詳細スキルシートタブ | **タブ集約実装済み**（タブ内にシート詳細を表示。未作成なら「新規作成」→保存時に自動紐付け。既存シートは要員編集画面で手動紐付け。編集・PDFは既存 `/skill-sheet` 画面へ遷移＝ルートは残置） | 解消 |
| マッチング一覧（`/matching`） | **実装済み**（閲覧専用・状態/未提案絞り込み・スコア0-100点表示） | 解消 |
| マッチングの承認/見送り | **実装済み**（要員詳細のマッチングタブで承認/見送り/差し戻し。見送り理由は任意） | 解消 |
| 提案管理一覧（`/proposal`） | `/proposal` に改名済み。**新7ステータス・次回アクション日・面談（x/y回目）表示・更新UI実装済み** | 解消 |
| `members` / `matchings` テーブル | `matchings` 新設・**`personnel` 拡張とも実装・devDB適用済み**（skill_sheet_id〔UNIQUE〕/name_initial/gender/birth_date追加。単価はAPI万円で公開） | 解消 |
| マッチング＝スキル文字列比較 | スキルマスタ（skill_id）方式で実装済み | 実装方式を正とする（決定済み） |

（案件一覧 `/ses/projects` は削除済み〔2026-08-17決定〕。案件詳細はメニュー外で残置し各所からリンク到達）

---

## 2. 実装済み: スキルシート管理

要件は [スキルシート要件](../requirements/skill-sheet-requirements.md) に反映済み（実装準拠）。
実DB結合確認済み（2026-08-17）。

### API（thyme。全POST・既存FUKURO共通ラッパー形式）

| パス | 内容 |
|------|------|
| `/skill-sheet/list` | 一覧（サーバー側ページング・ソート。検索条件はリクエストで受けられるが現状フロント絞り込み） |
| `/skill-sheet/detail` | 詳細 |
| ~~`/skill-sheet/entry`~~ | **撤去済み**（2026-08-17。要件廃止に伴いController・Service・Entity・テストを削除） |
| `/skill-sheet/edit` | 新規/更新兼用（IDの有無で分岐）。社員番号重複はエラーコードで返す |
| `/skill-sheet/draft` ／ `draft/get` ／ `draft/delete` | 下書きUPSERT・取得・削除 |

- gender / remotePreference 等のコード値は**int**のまま返す（`/ses/**` 系は文字列。系統間で不統一）
- 単価はAPI・画面が万円/月、DBが円/月（サーバー側で換算）
- 公開保存時はサーバー側が該当シートの下書きを**ユーザーを問わず全削除**

### 一覧画面の実装仕様

- 列: ステータス（未公開/公開済み）／氏名／社員番号／年齢（画面算出）／最寄駅／主なスキル／経験年数（経歴区分「経験者」のみ合算）／就業状況／勤怠提出元（管理者のみ）／最終更新日時
- 検索: 氏名・社員番号／就業状況／勤怠提出元（管理者のみ）＋条件クリア。
  **既知課題**: 絞り込みはフロント側暫定実装のため表示中ページにしか効かない
- 算出値（就業状況・経験年数・主なスキル）はソート対象外

### 残タスク

- E2Eテスト（Playwright導入済み・spec未作成）
- 検索のサーバー側移行
- タブ化方針（1章）への追従

---

## 3. 実装済み: 要員・案件・提案・マッチング（`/ses/**`）

実API接続済み。API形式は**REST**（GET/POST/PUT/PATCH、素のaxios）。認証は既存トークンをヘッダー付与。

### 要員（personnel）

- `GET /ses/personnel?type=&q=&status=`、`GET /ses/personnel/{id}`、`POST`、`PUT`、`GET /ses/personnel/{id}/matches`
- 区分: 自社社員/BP。ステータス: 稼働可能/稼働中/稼働不可。保有スキルはskill_id参照＋経験年数
- **2026-08-17拡張**: skill_sheet_id（UNIQUE・1シート1要員）／氏名イニシャル／性別（API文字列）／生年月日を追加。
  希望単価をAPI公開（万円/月。DBは円）。**PUTは全項目送信の非selective更新**（省略項目はクリアされる）
- **登録・編集画面・スキルタグ入力UIも実装済み**（/member/new・/member/:id/edit。サジェスト選択のみ・自由入力不可。
  スキルシートの手動紐付け選択ダイアログ付き）

### 案件

- `GET /ses/projects?q=&status=`、`GET /ses/projects/{id}`、`GET /ses/projects/{id}/candidates`
- 登録・更新APIは意図的に無し（起点はメール取り込み側の将来実装）
- ステータス: open/closed。単価レンジ・リモート区分・開始時期・面談回数等
- **案件一覧画面は削除**（2026-08-17決定。メール取り込み実装後の「案件メール一覧」が同じ `projects` の
  一覧になるため収斂させる）。**案件詳細（候補要員表示付き）はメニュー外で残置**し、
  マッチング一覧・提案・要員詳細からリンクで到達する。一覧APIは残す

### 提案

- `GET /ses/proposals?projectId=&personnelId=&status=&includeDone=`、`POST`、`PATCH`
- ステータス6値: 下書き→提出済み→面談→成約/見送り/取下げ（遷移ガードなし・後戻り自由）。
  提出時刻・結果確定時刻を記録。作成時にスコア・一致/不足スキルを `match_snapshot`（json）へ凍結
- **実施済み（2026-08-17 提案改修）**: ステータスを7値
  （0:下書き/1:提案メール送信済み/2:面談/3:先方返信待ち/4:完了/5:見送り/6:取下げ）へ変更。
  `interview_done_count`・`next_action_date` カラムを追加し、レスポンスに
  `projectTitle`・`projectInterviewCount` を追加（契約不整合も解消）。
  フロントは「面談（2/3回目）」表示・次回アクション日の表示/更新に対応。
  thyme/Lavenderともテスト・型チェック全PASS
- devDBへのALTER適用は**完了**（2026-08-17人間実行。proposals新カラム・matchingsテーブルとも適用を検証済み）。
  移行用ALTERファイルは適用完了に伴い削除し、`docs/ddl/schema-ses.sql`（変更履歴付き）に一本化した

### マッチング

- スコア = 一致スキル数 ÷ 案件側スキルタグ数（skill_id集合の一致）
- ハードフィルタ: 稼働可能時期 <= 案件開始時期／単価レンジの重なり（未指定は通過）
- 双方向（案件→候補要員／要員→適合案件）、スコア降順で上位20件。推薦理由はPhase2のLLM用の席のみ

### スキルマスタ

- `GET /ses/skills?q=` — 正規名＋別名に部分一致のサジェスト。Phase1はシードSQL投入（登録・管理APIなし）
- マスタ未登録語は `unknown_terms` に収集
- スキルシートのスキル（名前直接保持）とは独立で、現状ひも付かない

### DDL

thyme `docs/ddl/schema-ses.sql` が正（**17テーブル統合版**、2026-08-09統合）。
最新要件の `members` / `matchings` テーブル案とは構成が異なる（1章）。

---

## 4. 画面モックのみ: 営業メール取込

- `/project`（案件リード一覧・詳細編集）・`/staff`（要員リード一覧・詳細）をLavender上にダミーデータで実装
- リードのステータス: 未確認/確認済み/非表示。要員リードの氏名はイニシャルのみ保持
- メール取り込みバッチ・API・保存処理は未実装

---

## 5. 撤去・差し替え対象（要件変更で不要になった実装）

| 実装 | 経緯・方針 |
|------|------|
| `/skill-sheet/entry` API（thymeスタブ） | **撤去済み**（2026-08-17実施） |
| ログインAPIレスポンスへのロール追加（仮実装。Lavenderのログイン状態・IndexedDBにも保持） | 現在は一覧の勤怠提出元列・検索の表示制御に使用中のため**当面維持**。本認証のロール定義確定時に差し替える |
| サイドバー2大項目構成・`/ses/**` パス体系 | 新メニュー構成（大項目「SES」1つ・`/member` 等）への改修が必要 |

**権限出し分けの意図は消滅ではなく実装箇所が移る（2026-08-17 人間確認）**:
一般ログイン（メンバー）の主目的は**自分のスキルシートの編集**であり、
旧要件の「権限に応じた初期表示出し分け」は、タブ集約後の構成では
「一般ユーザーを自分のメンバー詳細（スキルシートタブ）へ誘導する」形で実装し直す。
一般ユーザーにも案件一覧の参照は許可する（2026-08-17決定。許可範囲は一元管理して切り替え容易にする）。

---

## 6. その他の記録

- **追跡不能な参照文書**: thyme `docs/ddl/schema-ses.sql:10` が `docs/design/phase1-api-contract.md`・
  `matching-design.md`・`db/phase1_ddl.sql` を参照しているが、いずれもローカル・git履歴のどこにも存在しない
  （未コミットのまま失われた可能性）。SES系APIの契約の実質の正は Lavender `src/api/ses/types.ts`。
  契約文書を復活させるかは要判断

- Excelインポートは2026-08-16に中止決定（利用頻度・工数回収の観点）。UI・導線は削除済み。
  再検討時の前提整理は社内資料に保存
- 画面文言は「職歴」→「経歴」に統一済み（2026-08-05）
- 印刷（PDF表示）は `window.print` による職務経歴書帳票（A4縦・氏名イニシャル化・フォント自動調整）
- スキルシート系とSES系で通信方式（共通ラッパーPOST vs REST）とコード値表現（int vs 文字列）が不統一。
  統一方針は要判断

---

## 7. 既知の課題・残タスク（2026-08-17時点）

### 要判断

- **案件単価の単位不整合の疑い**: `projects.unit_price_min/max` のDDLコメントは「万円に正規化」だが、
  テストデータは円相当を投入しており、マッチングの単価フィルタは要員側（円）と素の大小比較をしている。
  実データ投入（メール取り込み）前に**DBを円に統一するかコメント修正か**を確定する必要がある

### 小さな穴

- `PUT /ses/personnel/{id}` の `availableFrom` 書式不正が500になる（`birthDate` 同様の400化が望ましい）
- 要員詳細の `/matches` はスコア上位20件選定後に承認状態を合成するため、判断済みの組が圏外に落ちると
  タブから見えなくなる（マッチング一覧側では末尾保証あり）

### 未着手・進行中

- 営業メール取り込み（**実装中**。判定・LLM・DDLの前提は確定済み）
- **ロール実装**（仕様確定済み〔2026-08-17決定 #11〜#15〕。メール取り込み完了後に着手。
  本人紐付けはメンバーにログインユーザーIDを追加する方式）
- E2Eテスト（スキルシート系・SES系とも）

---

## 関連ドキュメント

- [MVP要件定義](../requirements/mvp-requirements.md)
- [Lavender組み込み要件](../requirements/lavender-integration.md)
- [スキルシート要件](../requirements/skill-sheet-requirements.md)
- [画面設計](../design/screen-design.md)
- [データモデル](../design/data-model.md)
- [意思決定・実装ログ](decision-log.md)
