# 意思決定・実装ログ

日付順の意思決定と実装の記録。各決定の詳細・最新状態は個別文書（データモデル／各要件／[実装現状の記録](implementation-status.md)）が正で、本書は経緯を追うための索引。

---

## 2026-08-16

- **Excelインポート中止を決定**。理由: 利用頻度が少なく開発工数を回収できない恐れ。関係者共有済み

## 2026-08-17

### 意思決定

| # | 決定 | 反映先 |
|---|------|--------|
| 1 | 上流の要件再整理（SESメニュー5項目・スキルシートのタブ集約・`4d8c655`）を**正としてマージ**。実装とのギャップは各文書に注記し、詳細は実装現状の記録に集約 | 全文書 |
| 2 | **DB構成**: `members` は新設せず既存 `personnel` を拡張。`matchings` は承認状態のみの薄いテーブル。スコアは都度計算（承認時スナップショットも持たない） | [データモデル 3・5章](../design/data-model.md) |
| 3 | **entry API（権限による初期表示出し分け）のスタブは撤去**。ログイン情報のロールは仮実装として当面維持。一般ログイン（メンバー）の主目的は自分のスキルシート編集で、誘導はタブ集約後の構成で実装し直す | [Lavender組み込み要件 9章](../requirements/lavender-integration.md) |
| 4 | **提案ステータスは上流粒度の7値**（下書き/提案メール送信済み/面談/先方返信待ち/完了/見送り/取下げ）。面談は1値とし**実施済み面談回数を別カラム**で保持（案件ごとに面談回数が変わるため）。**次回アクション日を追加** | [MVP要件定義 7章](../requirements/mvp-requirements.md) |
| 5 | **一般ユーザーにも案件一覧の参照を許可**。許可範囲（ロール×機能）は一元管理し切り替え容易にする | Lavender組み込み要件 9章 |
| 6 | **matchings設計確定**: 見送り理由は任意／承認・見送り操作は**メンバー詳細のマッチングタブ**（一覧は閲覧専用）／提案は自動作成せず「提案を作成」＋**未提案バッジ**／スコア表示は0-100点 | データモデル 5章 |
| 7 | **SES案件一覧画面は削除**（メール取り込み実装後の「案件メール一覧」が同じ `projects` の一覧になり収斂するため）。案件詳細はメニュー外で残置しリンク到達 | 実装現状の記録 3章 |
| 8 | **メンバーとスキルシートの紐付け**: タブ起点の新規作成で自動紐付け＋既存シートは要員編集画面で手動選択。1シート1メンバー（UNIQUE） | データモデル／実装現状の記録 |
| 9 | **単価の単位は全テーブル円/月に統一**（`projects` / `staff_candidates` のDDLコメント「万円」が誤りだったため修正。API境界のみ万円） | DDL変更履歴 |
| 10 | 小修正2件を承認: `availableFrom` 書式不正の400化／要員詳細 `/matches` への判断済み末尾保証 | 実装現状の記録 7章 |

### 意思決定（追加・ロール設計）

| # | 決定 | 補足 |
|---|------|------|
| 11 | ロールは**管理者／一般の2値**で確定（仮実装の値定義をそのまま正式化） | 認可方式の内部詳細は社内メモ管理 |
| 12 | 本人紐付けは**メンバー（要員）にログインユーザーIDを持たせる**方式。要員編集画面で設定し、一般ユーザーは自分のメンバー詳細（スキルシートタブ）へ誘導 | |
| 13 | 一般ロールの参照範囲: **自分のメンバー詳細・自分のスキルシート編集・案件メール一覧/詳細（参照のみ）**。マッチング・提案・要員一覧・取込実行・編集系は管理者のみ | |
| 14 | **勤怠提出元は本人には非表示**（管理者のみ参照・編集。API側で担保） | DDLコメントの想定どおり |
| 15 | **管理ユーザーの管理画面**（一覧・登録・編集・無効化）をロール実装に含める。無効化が機能するようログイン時の状態チェックも修正。自己降格・最後の管理者削除のガード付き | |

### 実装（いずれも実装リポジトリ側。テスト全PASS・型チェッククリーン）

- **Phase A**: entry API撤去／提案改修（7ステータス・`interview_done_count`・`next_action_date`・`matching_id`、契約不整合の解消、フロントの表示・更新UI）
- **メニュー再構成**: 大項目「SES」1つ・5メニュー化、パス改名（`/member` `/matching` `/proposal` `/project-mail` `/staff-mail`）、メンバー詳細の `?tab=` クエリ対応、案件一覧削除
- **matchings**: テーブル新設＋承認/見送りAPI＋マッチング一覧画面＋要員詳細タブの承認/見送りUI
- **personnel拡張**: `skill_sheet_id`（UNIQUE）/`name_initial`/`gender`/`birth_date` 追加、希望単価のAPI公開（万円）
- **要員登録・編集画面**: スキルタグ入力（マスタサジェスト選択のみ）、スキルシート手動紐付けダイアログ
- **スキルシートタブ集約**: メンバー詳細タブにシート詳細を表示、タブ起点新規作成→自動紐付け、暫定メニュー項目撤去
- **モック**: Excelインポートを削除（gh-pages `3b6ead8`）
- **DB**: 上記のスキーマ変更を開発DBへ適用・検証済み（提案・matchings・personnel）。単価コメントも同期
- **結合確認**: マッチング承認→提案作成の一巡を実DBで確認（全9シナリオOK）。personnel拡張分の結合確認も実施

### 実装（追加・メール取込パイプライン。テスト全PASS・型チェッククリーン）

- **取込バッチ**（thyme）: IMAP 2段階バッチFETCH・バウンス除外・LLM 3値分類＋構造化抽出（上限年齢含む）・
  スキルマスタ照合・履歴2階層（実行ヘッダ＋アカウント別明細）・スケジューラon/off＋手動実行API＋多重起動ガード。
  OpenAI API仕様書を事前整理（reasoning effort none必須＝コスト前提、Responses API採用、429の2種分岐 等）
- **リード編集API**: 案件参照・編集（単価はAPI万円へ統一）、要員候補CRUD（確認/非表示）、メール原文参照
- **画面実API化**: 案件メール・要員メール画面のモック撤去、取り込みボタン＋履歴ダイアログ
- devDBへ mail_import_runs / histories.run_id / projects.age_limit を適用済み
- **実IMAP・実OpenAI接続は未検証**（実値配置後に初回手動実行で確認する）
- 決定#19の商流抽出（enum v1）は**未実装**（次の取込スキーマ拡張で対応。prompt_versionを上げる）

### 実装（追加・2026-08-18未明。テスト全PASS・型チェッククリーン）

- **スキルマスタ初期シード＋再紐付け（A1）**: 実メール由来の未知語370語を全数分類
  （登録238・別名32・却下90・保留14）し開発DBへ投入。既存538案件の85%に1,928タグを再構築。
  マッチングが実データで稼働開始
- **ロール実装（決定#11〜15）**: 管理者/一般の2値、一般の本人スコープ（自分のメンバー詳細・自分のシート・
  案件メール参照のみ）、勤怠提出元の本人非表示、無効アカウントのログイン遮断、
  管理ユーザー管理画面（登録は一般のみ・自己降格/自己無効化/最後の管理者保護）、メニュー・タブの出し分け
- **商流・年齢の判定軸（決定#17〜19）**: 商流enum v1と個人事業主可否をLLM抽出・保存・編集画面に追加、
  マッチングに年齢・商流のハードフィルタ（判定不能は通過）。start_ym正規化を受信日基準の解決規則で改善
  （プロンプト版数 v2）
- **スキルマスタ育成画面（A2）**: 未知語トリアージ（登録/別名/却下/差し戻し・判断材料つきダイアログ・
  「迷ったら保留」明記）、マスタ一覧・統合（マージ）・全件再照合。登録/別名時は該当案件へ自動再紐付け

### 発見した既知課題（→ [実装現状の記録 7章](implementation-status.md)）

- 参照されている `phase1-api-contract.md` / `matching-design.md` / `db/phase1_ddl.sql` がどこにも存在しない（SES系API契約の実質の正はLavenderの `types.ts`）
  → **2026-08-17 解消**: 旧環境（WSL）から原本を復元しリポジトリへ追加（`e7feac5`）。いずれも8/17再設計以前の経緯資料
- 要員詳細 `/matches` は上位20件選定後に状態合成（末尾保証で対処済み）
- `PUT /ses/personnel` は全項目送信の非selective更新（省略項目はクリアされる仕様）

### 検討記録（マッチングロジック・実ユーザープロトタイプ突き合わせ）

- 実ユーザー作成のプロトタイプ（SES Auto Send 企業テスト）と現行ロジックを突き合わせ。**骨格は一致**（スキルで点数・待遇はOK/NG足切り＋理由表示・判定は人間）。表示は「総合点1軸＋条件別内訳テーブル」が目指す形
- 詳細: [プロトタイプ突き合わせ](../design/matching-prototype-alignment.md)

### 意思決定（追加・マッチング判定軸とスコープ）

| # | 決定 | 反映先 |
|---|------|--------|
| 16 | **メール送信は雛形生成まで**。送信は人間が内容確認のうえ手動（自動送信は過剰と判断しスコープ外） | [プロトタイプ突き合わせ 4章](../design/matching-prototype-alignment.md) |
| 17 | **年齢制限を判定軸に追加**。案件メールに記載があれば案件側に上限年齢を保持（取込時抽出・記載なし=制限なし） | 同上 |
| 18 | **商流はAI抽出方針を暫定採用**。要員側は `personnelType` 導出でAI不要、案件側の商流上限のみ既存LLM取込抽出に項目追加。実メール320通データセットで精度検証後に確定。正規化ルール定義が前提 | 同上 |
| 19 | **#17/#18を精度検証のうえ本採用確定**。project 171通で検証: 根拠原文の捏造0・再現率約95%・正規化一貫性約9割。商流enumはv1へ再設計（own_only/own_plus_support/first_ok/second_ok/unrestricted/unstated＋個人事業主可否は別フラグ、商流位置の自己申告は制限と分離） | [精度検証](../design/flow-age-extraction-verification.md) |

## 2026-08-18

### 意思決定

| # | 決定 | 反映先 |
|---|------|--------|
| 20 | **外国籍の判定軸を追加**。要員に「国内/外国籍」区分（任意入力・**本人には非表示**）、案件に外国籍可否（抽出昇格・プロンプト版数v3）。マッチングは判定不能通過のハードフィルタ。あわせて要員に個人事業主区分を追加し、保留中だった個人事業主フィルタを有効化 | 実装現状の記録 |
| 21 | 商流抽出の**整合チェック**を追加（原文根拠があるのにunstated判定になるLLM非決定性への対策。検知時1回リトライ） | 同上 |
| 22 | **スキル経験年数はまず表示のみ**（一致スキルに要員の年数を併記。判定には使わない）。判定への利用は**検討中＝相談のうえ決定**（案件側要求年数の抽出＋精度検証が前提） | [プロトタイプ突き合わせ 4.5](../design/matching-prototype-alignment.md) |
| 23 | パスワード長の不整合は**登録画面側に12文字上限を追加**して解消（既存ログイン画面は変更しない。上限拡大は将来のポリシー見直し時に両側同時） | 実装現状の記録 |
| 24 | **NGワードは判定軸に採用しない**（保持場所・判定対象の定義が必要になった時点で再検討） | [プロトタイプ突き合わせ 4章](../design/matching-prototype-alignment.md) |
| 25 | **マッチングスコア新仕様（上流2ab7445）の採用と詳細確定**。skillScore=必須80%＋尚可20%（片側0件はもう片方を100%扱い）。conditionChecksは7軸（個人事業主を追加）・各軸に理由文字列。境界値: 単価乖離5万円以内/開始遅れ1ヶ月以内/年齢超過5歳以内=WARNING・超えたらNG。稼働形態は対応表判定。**商流はOK/WARNING/NGの3値**（own_plus_support×BP=WARNING。OK/NG二値からの変更は上流同意済み）。null軸はOK＋理由「判定材料なし」。NG1件で対象外。conditionFactor=1.00/0.90/0.80/0.70。match_snapshotは**バージョン付きv2**（既存v1は移行せず従来表示）。実機検証反映: 単価は**対の値が揃えば部分判定**（要員min×案件max等。メール由来案件は上限のみ記載が多数派のため） | [API契約](../design/phase1-api-contract.md)・[データモデル](../design/data-model.md)・[プロトタイプ突き合わせ 4章1e](../design/matching-prototype-alignment.md) |
| 26 | **経験年数を判定に採用**（#22の未決部分を決定）。conditionChecksの**8軸目 `skillYears`（OK/WARNINGのみ・NGなし）**: 一致スキルに案件側要求年数があり要員の年数が不足すればWARNING1件＋不足スキルを理由に列挙。案件側要求年数は取込時LLM抽出（prompt v4）で `project_skills.required_years` に保持。**既存全案件は検証方式流用でバックフィル**。8軸化の上流仕様更新は商流3値と同様に進める | [API契約](../design/phase1-api-contract.md)・[プロトタイプ突き合わせ 4.5](../design/matching-prototype-alignment.md)・[精度検証](../design/skill-years-extraction-verification.md) |
| 27 | **スキルマスタで Spring と Spring Boot は別スキルとして維持する**（統合しない。案件原文「Spring N年」の年数はSpringタグに付き、Spring Boot保有要員の年数判定対象外になるが、区別を優先する判断） | スキルマスタ運用 |

### 検証記録（#22 経験年数の判定利用）

- 案件側の要求経験年数のLLM抽出を実メール171通で精度検証（商流#19と同方式）: 捏造0（全117エントリが本文逐語一致）・
  実質取りこぼし0・正規化一貫で**抽出は実用水準**。「目安」「程度」表現が多くハード足切りは実態より厳しくなるため、
  採用するならOK/NG＋理由表示（または減点）が適合 → 採否・判定方式は相談のうえ決定
- 詳細: [スキル経験年数の精度検証](../design/skill-years-extraction-verification.md)

### 実装（#20〜#23。テスト全PASS・型チェッククリーン）

- 外国籍・個人事業主: 要員2区分（本人非表示）＋案件の外国籍可否（抽出v3）＋マッチングフィルタ2本
  （個人事業主フィルタの保留解除含む）。開発DB適用済み
- 商流整合チェック（原文根拠×unstated矛盾の1回再抽出）、一致スキルへの経験年数併記（3画面）
- ロール実機結合確認の所見対応: 取込パネル管理者のみ表示／403時の画面骨組み非表示（5画面）／
  管理ユーザー登録のパスワード12文字上限
- 副産物: 全項目送信PUTの型必須化により、シート新規作成時の紐付け処理が新項目をクリアする潜在バグを検出・修正
- マッチング確認用の要員テストデータ10件（判定軸の全バリエーション）を開発DBに常置

## 2026-08-19

### 意思決定

| # | 決定 | 反映先 |
|---|------|--------|
| 28 | **資格（CCNA/CCNP等）はスキルマスタに同居させる**（資格専用カテゴリ・別テーブルは新設しない。複雑化回避を優先し、区別が必要になった時点で再検討）。なおベンダー名スキル（Cisco等）は「該当ベンダー機器の経験」の略記として妥当と確認（実メール用例・案件21通で検証） | スキルマスタ運用 |
| 29 | **/ses/** の未知enum文字列は読み書きとも一律400に統一**（B5テスト作成時の調査で、即400/サイレント無視/絞り込み解除/サービス委譲の4通りに割れていた実装のばらつきを解消。フロントは固定選択肢のみ送るため未知値=バグか手打ちであり、黙って通す方が事故になる）。あわせてエラー応答規定（バリデーション・未知enum=400空ボディ／対象なし=404／業務エラー=400）を契約に明文化 | [API契約](../design/phase1-api-contract.md) |
| 30 | **マッチングフロー改修: マッチング結果画面を新設**（2026-08-19人間指示・ホワイトボードメモ起点）。一覧2箇所（メンバー詳細マッチングタブ/マッチング一覧）は列=案件名・点数・一致スキル・不足スキル・ステータス・見送りボタンの縦リストにし、行クリックで結果画面へ。結果画面は左=案件×要員の対比表（スキル〇△×・条件8軸の生値対比・劣位側ハイライト）、右=重複メール縦リスト（会社名/アドレス/件名/受信日・案件メール詳細リンク・**提案ボタン=経由メール選択**、フレーム内スクロール）＋フレーム外に見送りボタン。**承認は3段階維持**（承認ボタンは結果画面に配置）。提案に経由メールを記録（proposals.via_mail_id・提案先導出はvia優先）。**同一案件の自動グルーピングは精度検証後に導入**（それまで右側は元メール1件） | [API契約](../design/phase1-api-contract.md) |
| 31 | **要員候補（未昇格BP）もマッチング対象に追加**。staff_candidate_skills新設（マスタ照合タグ・既存分バックフィル）、matchingsに候補ID対応（見送りも記録=人間指示「折角だから」）、**昇格はイニシャル名で自動**（promote API・タグコピー・matchings付け替え・冪等）、結果画面の提案ボタンは候補なら自動昇格→提案作成。**性能対策を同時装備**（人間決定）: ①共通スキル1件以上のSQL前置フィルタ（0点ペア非表示化） ②鮮度=「開始時期が過去」or「受信1ヶ月超」の案件を評価対象外（手動登録は常に対象） ③一覧のサーバ内キャッシュ（更新系で無効化+TTL） | [API契約](../design/phase1-api-contract.md) |
| 32 | **C7（スキルシート編集の基本情報タブ削除）は単純削除できず、個人属性をメンバー（`personnel`）側へ移設する方針に変更**。基本情報13項目のうち氏名/性別/生年月日/最寄駅/希望単価の5項目は`personnel`に同等カラムがあり単純削除、社員番号/フリガナ/都道府県/市区町村/最終学歴/希望勤務地/リモート希望/勤怠提出元の8項目は`personnel`へ移設する。移設後スキルシート編集は3タブ（スキル・資格／経歴／自己PR）になる | [個人属性のメンバー移設 設計](../design/personnel-attribute-migration.md) |
| 34 | **マッチングに `location`（勤務地の地方）軸を追加し9軸化する**（2026-08-19人間指示）。`workStyle` 軸がリモート希望しか見ておらず「関東常駐×関西在住×出社のみ可」がOKで通過していた穴を塞ぐ。**8地方区分で一致必須**とし、不一致はNG（＝候補から除外）。フルリモート案件（`remote_type=0`）は例外としてOK、併用案件は低頻度出社キーワード（基本リモート・原則リモート等）があればWARNINGに緩める。ただし「地方不可」等の**否定キーワードは最優先**で判定する（低頻度出社なのに地方在住を断る案件が実在するため）。地名→地方の解決は**取込済みデータから起こす辞書方式**（駅マスタ導入・LLM抽出は不採用）。**同名地名（本町・府中・大手町・淡路）は辞書から除外し「判定材料なし＝OK」に倒す**（誤NGは画面から消えて人間が気付けないが、誤OKは営業が弾けるという非対称性による）。実装は個人属性移設の手順5と同時に行う | [location軸 設計](../design/matching-location-axis.md) |
| 33 | **決定#32（個人属性のメンバー移設）の実装方針4点を確定**。印刷帳票・スキルシート詳細表示は**フロントで`personnel`/`skill_sheets`の2API結合方式**、**スキルシートはメンバー起点でのみ作成を必須化**（単体作成は不可）、社員番号UNIQUE移設に伴うエラー発生箇所変更は要判断事項ではなく実装作業として整理、**性別・生年月日は全メンバー必須化**（`personnel`側を`NOT NULL`化。適用前に既存データ補完が必要） | [個人属性のメンバー移設 設計 7章](../design/personnel-attribute-migration.md) |
| 35 | **要員候補（staff_candidates）の名寄せを導入**（2026-08-20人間承認）。同一人物が取込のたび別レコードになり、マッチング一覧に同じ行が並ぶ問題への対処（実データ996件中 T.M が55件・K.M が62件）。キーは **イニシャル＋最寄駅（正規化トークン）＋年齢**。**`affiliation`（所属）はキーに使わない** — 実データ検証で「弊社社員/弊社正社員/弊社プロパー/自社社員」等の表記ゆれにより44グループ余計に割れ、正規化しても解消しないことを確認（996件→405グループ／所属込みだと449）。最寄駅の正規化は案件側 F1 の勤務地前処理を再利用。年齢は両方非NULLのときのみ一致必須。イニシャルまたは最寄駅がNULL/空のレコードは畳まない。方式は案件側と対称の**永続列方式**（`staff_candidates.group_id`＋バックフィルAPI）とし、オンメモリ畳み込みは不採用（代表交代時に承認・見送りが孤立するため）。**昇格は代表からのみ可**（非代表IDは400）で、昇格時はグループ全体を昇格済みにする | [要員候補の名寄せ 精度検証](../design/20260820_1616_staff-candidate-grouping-accuracy.md) / [API契約](../design/phase1-api-contract.md) |
| 36 | **マッチング結果画面の右カラムを上下2フレーム化**（2026-08-20人間指示・ホワイトボードメモ起点）。上=案件メール／下=要員メール（候補ペアのみ。メンバーペアは要員側フレーム自体を非表示）。**どちらもラジオ相当の単一選択**（選択行は背景色で区別）とし、**各行にあった提案ボタンは廃止してフレーム外に集約**。提案ボタンは候補ペアなら両方選択、メンバーペアなら案件側選択で活性。初期状態は未選択。要員側の経由メールも記録する（`proposals.via_staff_mail_id` 追加）。ただし**提案先（destinationName/Email）の導出ロジックは変更せず**従来どおり案件側 via_mail_id 優先→projects.mail_id、要員側は記録のみ | [API契約](../design/phase1-api-contract.md) |
| 37 | **`hasProposal` は「進行中の提案があるとき」だけ true に変更**（2026-08-20人間指示）。従来はステータスを問わず提案の有無で判定していたため、一度提案すると見送られた後も同じ案件×要員に二度と提案できなかった。阻止したいのは**同一案件×同一要員の進行中の二重提案だけ**であり、完了・見送り・取下げの後の再提案（別ルート・時間差）は正当な運用。線引きは提案一覧の `includeDone` と同じ（won/lost/withdrawn 以外が進行中）。なお `proposals` に (project_id, personnel_id) のUNIQUE制約は元々無く、API側にも重複チェックは無い（制限していたのはUIのみ） | [API契約](../design/phase1-api-contract.md) |
| 38 | **提案メールのテンプレート作成機能を追加**（2026-08-20人間指示）。メンバー詳細「提案状況」タブの更新ボタン左に「案件側テンプレート」「要員側テンプレート」の2ボタンを置く。案件側＝案件を出したBPへ**要員を紹介**、要員側＝要員を出したBPへ**案件を紹介**。生成は**穴埋め方式**（LLM不使用＝ランニングコストゼロ。決定#17の方針を踏襲）。**ダイアログに表示してコピーするだけで保存しない**（`proposals.proposalText` には触れない。決定#16「雛形生成まで・送信は人間が手動」の範囲）。文面の置き場所は当面**フロントの定数ファイル1本に集約**し、文面本体／差し込み値の組み立て／置換の3層に分けて実装する（文面が固まった後に静的ファイルやDBへ供給元を差し替えられるようにするため） | — |
| 39 | **提案ステータスのセレクトから「下書き」を除外**（2026-08-20人間指示）。セレクトの選択肢は移動先ステータスだけあれば足りるという判断。ラベル定義・進捗ステッパー1段目・一覧の表示では `draft` を引き続き使う。対象はカードの更新フォームと提案一覧の両方。API側は従来どおり後戻り自由（厳密なガードは設けない） | — || 40 | **要員候補一覧（要員メール一覧）の重複表示を他画面へ寄せる**（2026-08-20人間指示）。名寄せの結果を要員候補一覧にも出す。**案件メール一覧と同じ扱い**とし、一覧は畳まず全件表示したうえで `duplicateCount` 2以上に「重複N件」バッジを表示する（代表のみに絞るのはマッチング評価の母集団だけ、という 案件側の切り分けを踏襲）。API契約の重複件数の規定を「案件行を返すAPI」から要員候補一覧へ広げ、thyme 側に `StaffCandidateShowData.duplicateCount` を追加する。フロント（`types.ts` / `staffCandidateApi.ts`）は受け皿とフォールバックが実装済みのため追加改修は不要 | [API契約](../design/phase1-api-contract.md) |
| 41 | **社員番号（`personnel.employee_code`）を自動採番する**（2026-08-20人間指示）。社員は連番（現状は2桁で収まっているが**理論上は社員増で桁が増える**前提とし、桁固定にしない）、BPは `BPnnnnnn` 形式で**全社通しの連番**を採る。取引先ごとの採番はしない。**既存の9000番台（E3のレイアウト検証用に投入した10件）はそのまま残す**。実装上の前提: ①列が `varchar(4)` のため拡張が必要 ②フロントの「半角数字4桁ちょうど」検証（`personnelEdit.tsx`）は桁可変＋2形式許容へ変更 ③社員/BPの振り分けは既存の `personnel.personnel_type`（0:社員/1:管理BP）を使う ④採番は UNIQUE 事前検証を持つ `PersonnelService.assertEmployeeCodeAssignable()` へ寄せ、書き込みオーナーを1箇所に保つ | — || 42 | **一覧3画面にページングを入れ、絞り込みを強化する**（2026-08-20人間指示）。全件表示のため「詳細へ行って戻ると最上部までスクロールが戻り見失う」問題への対処。**デフォルト100件・選択肢は 50/100/200/500**（FUKURO側 `components/table.tsx` の `[10,25,50,100]` は据え置き、SES3画面は件数の桁が違うため別の選択肢を持つ）。**ページングはクライアント側**（絞り込み後の母数に対して切る／マッチング一覧のサーバー内キャッシュの効きを落とさない／全件JSONの転送コストは現状すでに払っている）。**絞り込みは従来どおりサーバー側**。あわせて**一覧の状態（ページ・表示件数・絞り込み条件）をURLクエリへ保持**し、ブラウザの戻るで復元する（現状は全て `useState` でURLに何も乗っておらず、戻ると絞り込みごと初期化される。`personnelDetail` の `?tab=` と同じ作法）。絞り込み項目は案件=フリーワード範囲拡大・勤務地・必須スキル・単価/開始時期レンジ・リモート/商流/個人事業主/外国籍、要員=昇格済み有無・所属/最寄駅/スキル個別・年齢/単価/開始時期レンジ・重複有無、マッチング=案件名・メンバー/候補名・点数下限・メンバー/候補の別 | [修正タスクリスト J](20260818_1758_fix-tasks.md) || 43 | **OpenAI呼び出しのレート制御を「ヘッダ駆動の事前減速＋共有クールダウン」にし、並列数は実測から決める**（2026-08-21人間指示）。2026-08-19に並列4で429を踏み暫定で2へ落としたが、**上限を決めているのは並列数ではなくTPM**。実測TPM 200,000・1リクエスト約6,000トークンで天井は毎分約33リクエストであり、並列数は「どれだけ速く天井にぶつかるか」を決めるだけ。ペーシング無しに並列だけ上げるとバーストが強くなる。方式は `x-ratelimit-remaining-tokens` を見て**429を踏む前に全スレッドを減速**させ、それでも踏んだ場合は**共有の「次に投げてよい時刻」で全体を止める**二段構え。送信前のトークン数見積もりが要らない点でトークンバケット方式より軽く、api_specの推奨とも一致する。適正並列数は N = 33 × T ÷ 60（T=1リクエストの所要秒数）で決まるが**T が未計測**のため、**制御の実装と同時に所要時間と usage トークン数の計測を仕込み**、実データで決める | [修正タスクリスト M](20260818_1758_fix-tasks.md) |
| 44 | **メール取込の環境横断排他（DB側ロック）は入れない＝現状維持**（2026-08-24人間判断）。多重起動の排他は `MailImportService` のプロセス内 `AtomicBoolean` のみで、別環境からの同時実行は物理的に可能だが、①画面の実行ボタンはDB由来の実行中表示（`mail_import_runs.finished_at IS NULL`）でdisableされ環境をまたいで防がれる ②残る競合窓はポーリング反映までの数秒に別環境で同時に叩くケースのみで社内ツールでは現実性がない ③万一同時実行してもMessage-ID重複キーが最終防衛でデータは壊れない（実害は同一メールへの二重LLM呼び出しのトークン浪費のみ）、という理由による。悪意ある操作は想定しない | — |

---

## 記入ルール

- 決定は「誰でも1行で結論が分かる」粒度で書き、詳細は反映先文書へリンクする
- 秘密情報（認証情報・接続先・認可の内部仕様）は書かない（公開リポジトリのため）
